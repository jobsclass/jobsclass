# 🧪 유저 시나리오 기반 테스트 & 보완 계획

## 📋 테스트 시나리오

### 👤 시나리오 1: 신규 파트너 회원가입 → 서비스 등록 → 발행
**목표**: 파트너가 가입부터 서비스 판매까지 완료

**플로우**
1. 메인 페이지 접속 → "파트너로 시작하기" 클릭
2. 회원가입 (파트너 타입)
3. 온보딩 (사업자등록증 업로드)
4. 대시보드 접속
5. 서비스 등록
6. 서비스 발행
7. 마켓플레이스에서 확인

**체크 포인트**
- [ ] 회원가입 시 파트너 타입 자동 선택
- [ ] 온보딩 필수 항목 검증
- [ ] 서비스 등록 폼 (6가지 타입, 8개 카테고리)
- [ ] 서비스 발행 전 검증
- [ ] 마켓플레이스 노출 확인

---

### 👥 시나리오 2: 신규 클라이언트 회원가입 → 서비스 검색 → 구매
**목표**: 클라이언트가 서비스를 찾아 구매 완료

**플로우**
1. 메인 페이지 접속 → "서비스 둘러보기" 클릭
2. 마켓플레이스에서 카테고리/타입 필터
3. 서비스 상세 페이지
4. 회원가입 (클라이언트 타입)
5. 서비스 구매 (결제)
6. 주문 확인

**체크 포인트**
- [ ] 비로그인 상태에서 마켓플레이스 접근
- [ ] 필터 작동 (6개 타입, 8개 카테고리)
- [ ] 서비스 상세 정보 표시
- [ ] 로그인 유도 (회원가입 링크)
- [ ] 결제 플로우
- [ ] 주문 완료 후 대시보드

---

### 🔄 시나리오 3: 파트너 - 클라이언트 견적 요청/응답
**목표**: 견적 요청 시스템 완전 작동

**플로우**
1. 클라이언트: 니즈 등록
2. 파트너: 니즈 확인 → 견적 제안
3. 클라이언트: 견적 비교 → 선택
4. 계약 체결
5. 메시지로 소통

**체크 포인트**
- [ ] 니즈 등록 폼 (카테고리, 예산, 기한)
- [ ] 파트너 견적 제안 폼
- [ ] 클라이언트 견적 비교 UI
- [ ] 계약 체결 프로세스
- [ ] 실시간 메시지 연동

---

### 👨‍💼 시나리오 4: 관리자 파트너 승인 → 서비스 관리
**목표**: 관리자가 플랫폼을 효율적으로 관리

**플로우**
1. Admin 로그인
2. 파트너 승인 대기 목록 확인
3. 사업자등록증 확인 → 승인/거절
4. 서비스 관리 (게시/보관)
5. 푸터 설정 변경

**체크 포인트**
- [ ] Admin 권한 체크
- [ ] 파트너 승인 프로세스
- [ ] 서비스 상태 변경
- [ ] 푸터 설정 저장 및 반영
- [ ] 통계 정확성

---

## 🔍 발견된 문제점

### 🚨 Critical Issues

#### 1. **서비스 등록/수정 페이지 - 타입/카테고리 불일치**
**문제**: 기존 10개 타입 → 6개 타입으로 변경했는데, 등록 폼이 아직 업데이트 안 됨
**영향**: 파트너가 서비스를 등록할 수 없음
**파일**: 
- `app/dashboard/services/new/page.tsx`
- `app/dashboard/services/[id]/edit/page.tsx`

#### 2. **서비스 상세 페이지 - 타입 라벨 표시**
**문제**: 서비스 타입 표시 시 old 타입명 사용
**영향**: UI에서 타입명이 깨져 보임
**파일**: `app/marketplace/products/[id]/page.tsx`

#### 3. **파트너 대시보드 - 타입/카테고리 필터**
**문제**: 대시보드 서비스 목록 필터가 구 타입 사용
**영향**: 필터 작동 안 함
**파일**: `app/dashboard/services/page.tsx`

#### 4. **products 테이블 - type 컬럼 체크 제약**
**문제**: DB에서 old 타입만 허용하는 CHECK constraint 있을 가능성
**영향**: 신규 타입으로 서비스 등록 불가
**파일**: DB 스키마 확인 필요

#### 5. **user_profiles 테이블 - role vs is_admin**
**문제**: Admin 체크 시 `role = 'admin'` vs `is_admin = true` 혼용
**영향**: Admin 권한 체크 실패 가능
**파일**: 
- `app/admin/layout.tsx` (is_admin 사용)
- `app/admin/page.tsx` (role 사용)

### ⚠️ Important Issues

#### 6. **이미지 필드명 불일치**
**문제**: `image_url` vs `thumbnail_url` 혼용
**영향**: 서비스 썸네일 표시 안 될 수 있음
**파일**: 여러 페이지

#### 7. **메인 페이지 푸터 - 정적 데이터**
**문제**: 푸터가 아직 `site_settings`에서 데이터를 가져오지 않음
**영향**: Admin에서 푸터 변경해도 반영 안 됨
**파일**: `app/page.tsx`

#### 8. **가격 필드명 불일치**
**문제**: `price` vs `base_price` vs `discount_price` 혼용
**영향**: 가격 표시 오류 가능
**파일**: 여러 페이지

---

## 🛠️ 수정 계획

### Phase 1: Critical Issues (즉시 수정)
1. ✅ 서비스 등록/수정 폼 - 6개 타입, 8개 카테고리 적용
2. ✅ 서비스 상세 페이지 - 타입 라벨 수정
3. ✅ 파트너 대시보드 - 필터 수정
4. ✅ DB 스키마 - CHECK constraint 확인 및 수정
5. ✅ Admin 권한 체크 - 통일 (is_admin 사용)

### Phase 2: Important Issues
6. ✅ 이미지 필드명 통일 (image_url 사용)
7. ✅ 메인 페이지 푸터 - 동적 데이터 연동
8. ✅ 가격 필드명 통일 (base_price, discount_price)

### Phase 3: Polish
9. ✅ 에러 메시지 개선
10. ✅ 로딩 상태 개선
11. ✅ 빈 상태 UI 개선
12. ✅ 반응형 레이아웃 확인

---

## 📝 작업 순서

1. DB 스키마 확인 및 수정
2. 서비스 등록/수정 폼 업데이트
3. 서비스 관련 모든 페이지 수정
4. Admin 권한 체크 통일
5. 푸터 동적 데이터 연동
6. 전체 테스트 및 검증

---

## ✅ 테스트 체크리스트

### 기능 테스트
- [ ] 파트너 회원가입 → 서비스 등록 → 발행
- [ ] 클라이언트 회원가입 → 서비스 검색 → 구매
- [ ] 견적 요청 → 제안 → 계약
- [ ] Admin 파트너 승인 → 서비스 관리

### UI/UX 테스트
- [ ] 메인 페이지 모든 섹션 표시
- [ ] 마켓플레이스 필터 작동
- [ ] 서비스 상세 페이지 정보 표시
- [ ] 대시보드 통계 정확성
- [ ] Admin 페이지 모든 기능

### 데이터 일관성
- [ ] 타입/카테고리 일관성
- [ ] 이미지 필드 일관성
- [ ] 가격 필드 일관성
- [ ] 권한 체크 일관성

---

이제 순차적으로 수정을 시작하겠습니다.
